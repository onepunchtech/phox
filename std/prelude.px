let
  id : a -> a = \x => x
  const : a -> b -> a = \x _ => x
  flip : (a -> b -> c) -> b -> a -> c = \f x y => f y x
  compose : (b -> c) -> (a -> b) -> a -> c = \f g x => f (g x)
in { id = id, const = const, flip = flip, compose = compose }
